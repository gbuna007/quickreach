<div class="row d-flex justify-content-center">
  <div class="col-7">
    <%= simple_form_for(template, html: { class: "mb-3"}, data: {new_template_target: "submit",
        action: "submit->new-template#create"}) do |f| %>
      <%= f.input :name %>
      <%= f.input :subject %>
      <%= f.input :body %>
      <%= f.submit %>
    <% end %>
  </div>

  <div class="col-4 tutorial-card border border-dark p-2 me-2">
    <h3>Take Note!</h3>
    <p>In order to populate the right information, please use the following formats, <strong><em><u>wrapped in double arrows!!</u></em></strong><%= "<<variable_name>>" %></p>
    <p>-Variable names: </p>
    <p>account_name: Account Name, e.g. Grab</p>
    <p>contact_fn: Contact's first name</p>
    <p>account_category: Account's category, e.g. transportation,finance, e-commerce</p>
  </div>
</div>
