<div class="row d-flex justify-content-center">
  <div class="col-7">
    <%= simple_form_for(template, html: { class: "mb-3"}, data: {new_template_target: "submit",
        action: "submit->new-template#create"}) do |f| %>
      <%= f.input :name %>
      <%= f.input :subject %>
      <%= f.input :body %>
      <%= f.submit %>
    <% end %>
  </div>
  <div class="col-4 border border-dark p-2 me-2">
    <h3>Note on Variables:</h3>
    <p>In order for QuickReach to populate the right information, please use the following variables in your body wrapped in << var name >>:</p>
    <p>account_name: Account Name, e.g. Grab</p>
    <p>contact_fn: draft.trigger.contact.first_name</p>
    <p>account_category: draft.trigger.account.category</p>
  </div>
</div>
